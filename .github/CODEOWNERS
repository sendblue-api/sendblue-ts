# Execution-sensitive files. Changes here run code on CI runners and on
# developer machines, so they need review from the owners below (PLAT-5343).
# Owners are this repo's maintainers with write access.
# Enforcement requires a pull-request rule with "Require review from Code
# Owners" on the branch ruleset. This repo has no pull-request rule yet.

# CI workflows, git hooks, editor-run tasks
/.github/                         @lookevink @ricardoasilva92
/.husky/                          @lookevink @ricardoasilva92
/.vscode/                         @lookevink @ricardoasilva92

# Dependency manifests and package-manager configuration
/package.json                     @lookevink @ricardoasilva92
/yarn.lock                        @lookevink @ricardoasilva92

# Bootstrap scripts and development tooling
/.devcontainer/                   @lookevink @ricardoasilva92
/scripts/                         @lookevink @ricardoasilva92
